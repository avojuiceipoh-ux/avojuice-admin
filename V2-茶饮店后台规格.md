# 茶饮店完整后台规格（V2 设计依据）

> 调研对象：Loyverse、Square for Restaurants、Toast、StoreHub（MY）、MenuSifu / Ginger / Eats365 / Biyo 等专注奶茶/咖啡的垂直 POS，以及国内（CSDN、引瓴数智、纳客）对奶茶店中后台的功能梳理。
>
> 输出原则：**全建 schema，分期上线**。V1 只跑得动的几个模块（订单、菜单、库存、报表、会员雏形），其余字段先占位，标注「未来再做」。

---

## 1. 完整功能模块清单（Sidebar IA）

```
1. 仪表板 Dashboard
   ├─ 今日概览        — 实时营收/订单数/客单价/毛利
   ├─ 销售趋势        — 7/30/90 日折线，对比环比
   └─ 异常提醒        — 低库存/损耗超标/门店离线/退款异常

2. 订单 Orders
   ├─ 全部订单        — 列表 + 筛选（堂食/自取/外卖）
   ├─ 退款 / 作废     — 单独入口，必须授权
   ├─ 在线订单        — 顾客 App 来单，KDS 状态同步
   └─ 外卖平台单      — Grab/Foodpanda/ShopeeFood（V1 占位）

3. 菜单 Menu
   ├─ 产品 Products       — 名称、故事、图、SKU
   ├─ 变量 Variants       — 杯型 / 茶基 / 糖度 / 冰度
   ├─ 加料 Modifiers      — 珍珠/椰果/布丁/燕麦奶
   ├─ 分类 Categories     — 鲜榨果汁/奶昔/水果茶/季节限定
   ├─ 套餐 Combos         — 买 X 送 Y / 套餐捆绑（未来再做）
   └─ 时段菜单 Schedules  — Happy Hour、早鸟（未来再做）

4. 库存 Inventory
   ├─ 原料 Items          — 原料级（鳄梨、芒果、珍珠、糖浆）
   ├─ 配方 Recipes        — 1 杯 = 多少 g 鳄梨 + 多少 ml 牛奶
   ├─ 进货 Purchase Orders— 供应商、单价、到货
   ├─ 损耗 Wastage        — 类型（变质/打翻/试做/客诉）
   ├─ 盘点 Stock Count    — 期初/期末，差异分析
   └─ 转仓 Transfers      — 多门店调拨（未来再做）

5. 顾客 Customers
   ├─ 顾客列表        — 手机号、消费总额、上次到店
   ├─ 会员等级        — Silver/Gold/Platinum
   ├─ 积分规则        — 1 RM = X 分
   ├─ 优惠券 / Codes  — 单码 / 批量 / 一次性
   └─ 营销活动        — 推送、生日券、流失唤回（未来再做）

6. 员工 Employees
   ├─ 员工列表 + 角色  — Owner/Manager/Cashier/Barista
   ├─ 班次 Shifts      — 排班表
   ├─ 打卡 TimeEntry   — Clock in/out，工时
   └─ 权限 Roles       — 颗粒到「能否退款 / 改价」

7. 报表 Reports
   ├─ Sales summary       — 总览
   ├─ Sales by item       — 单品
   ├─ Sales by category   — 品类
   ├─ Sales by hour       — 时段（茶饮店必看）
   ├─ Sales by employee   — 员工业绩
   ├─ Receipts            — 单据流水
   ├─ Discount usage      — 折扣使用
   ├─ Wastage report      — 损耗（茶饮店命脉）
   └─ Tax / SST report    — SST 6% 汇总

8. 门店 Stores                       (V1 单店，但 schema 多店)
   ├─ 门店列表
   ├─ 营业时间
   └─ 设备 / 打印机

9. 设置 Settings
   ├─ 商户资料 / Logo / SSM
   ├─ 支付方式 Payment Types
   ├─ 税率 SST
   ├─ 收据模板 + 打印机
   ├─ 积分 / Loyalty 规则
   ├─ 功能开关 Feature Toggles
   ├─ 账单 & 订阅 Billing
   └─ 集成 Integrations（POS 硬件、外卖、会计）
```

---

## 2. 数据库 schema 设计建议

> 所有表都带 `id uuid PK`、`store_id`、`created_at`、`updated_at`、`deleted_at`（软删除）。下文省略这些公共字段。

### Product（产品 SPU）
```sql
CREATE TABLE products (
  name              VARCHAR(120)   NOT NULL,
  slug              VARCHAR(120)   UNIQUE,
  category_id       UUID           REFERENCES categories,
  story             TEXT,                          -- 品牌故事，顾客 App 用
  short_desc        VARCHAR(200),
  base_price        DECIMAL(10,2)  NOT NULL,       -- 基础杯型价
  base_cost         DECIMAL(10,2),                 -- 估算成本，库存扣减后会算实际值
  sku               VARCHAR(50)    UNIQUE,
  barcode           VARCHAR(50),
  image_url         TEXT,
  gallery_urls      JSONB,                         -- 多图
  is_available      BOOLEAN        DEFAULT true,
  is_featured       BOOLEAN        DEFAULT false,
  tax_id            UUID           REFERENCES taxes,
  prep_time_sec     INT            DEFAULT 180,    -- KDS 用
  tags              JSONB,                         -- ['halal','vegan','seasonal']
  nutrition         JSONB                          -- 卡路里/糖分（未来再做，先留字段）
);
```

### ProductVariant（变量 — 影响价格的强属性）
```sql
CREATE TABLE variant_groups (
  product_id        UUID NOT NULL REFERENCES products,
  name              VARCHAR(50),                   -- '杯型' / '茶基'
  selection_type    VARCHAR(20) DEFAULT 'single',  -- single / multiple
  is_required       BOOLEAN DEFAULT true,
  sort_order        INT
);

CREATE TABLE variant_options (
  variant_group_id  UUID NOT NULL REFERENCES variant_groups,
  name              VARCHAR(50),                   -- 'M' / 'L' / '红茶' / '绿茶'
  price_delta       DECIMAL(10,2) DEFAULT 0,       -- 相对 base_price 的差额
  cost_delta        DECIMAL(10,2) DEFAULT 0,
  is_default        BOOLEAN DEFAULT false,
  is_available      BOOLEAN DEFAULT true,
  sort_order        INT
);
```

> 糖度（0/30/50/70/100）、冰度（去冰/少冰/正常/热）也走这张表，只是 `price_delta = 0`。

### Modifier（加料 — 一般可选，影响价格）
```sql
CREATE TABLE modifier_groups (
  product_id        UUID REFERENCES products,      -- null = 全局共享
  name              VARCHAR(50),                   -- '加料'
  selection_type    VARCHAR(20) DEFAULT 'multiple',
  is_required       BOOLEAN DEFAULT false,
  min_select        INT DEFAULT 0,
  max_select        INT DEFAULT 5,
  sort_order        INT
);

CREATE TABLE modifiers (
  modifier_group_id UUID NOT NULL REFERENCES modifier_groups,
  name              VARCHAR(50),                   -- '珍珠' / '椰果'
  price             DECIMAL(10,2) DEFAULT 0,       -- +RM 1.00
  cost              DECIMAL(10,2) DEFAULT 0,
  inventory_item_id UUID REFERENCES inventory_items,
  is_available      BOOLEAN DEFAULT true,
  sort_order        INT
);
```

> 关键区分：**Variant = 必选、有限选项、定义产品本体**；**Modifier = 可选、多选、增项**。两套表分开做，比塞一张表干净得多，茶饮店痛点就在这里。

### Category
```sql
CREATE TABLE categories (
  name              VARCHAR(50),
  parent_id         UUID REFERENCES categories,    -- 二级分类（V1 不用，schema 留）
  color_hex         CHAR(7),                       -- POS 按钮颜色
  image_url         TEXT,
  sort_order        INT
);
```

### Order / OrderItem
```sql
CREATE TABLE orders (
  order_no          VARCHAR(20) UNIQUE,            -- A001 / B001 顺序号
  store_id          UUID,
  customer_id       UUID REFERENCES customers,
  employee_id       UUID REFERENCES employees,
  channel           VARCHAR(20),                   -- pos / app / grab / foodpanda
  order_type        VARCHAR(20),                   -- dine_in / takeaway / delivery
  status            VARCHAR(20),                   -- open/preparing/ready/completed/voided/refunded
  subtotal          DECIMAL(10,2),
  discount_total    DECIMAL(10,2) DEFAULT 0,
  tax_total         DECIMAL(10,2) DEFAULT 0,
  service_charge    DECIMAL(10,2) DEFAULT 0,       -- 占位
  rounding          DECIMAL(10,2) DEFAULT 0,       -- MY 5sen rounding
  total             DECIMAL(10,2),
  paid_total        DECIMAL(10,2),
  payment_status    VARCHAR(20),
  note              TEXT,
  voided_at         TIMESTAMP,
  voided_by         UUID,
  void_reason       TEXT
);

CREATE TABLE order_items (
  order_id          UUID NOT NULL REFERENCES orders,
  product_id        UUID NOT NULL,
  product_name      VARCHAR(120),                  -- 冗余存：未来改名不影响历史
  quantity          INT DEFAULT 1,
  unit_price        DECIMAL(10,2),                 -- 含 variant 调整后的单价
  unit_cost         DECIMAL(10,2),
  variants          JSONB,                         -- [{group:'杯型',option:'L',delta:1}]
  modifiers         JSONB,                         -- [{name:'珍珠',price:1.00}]
  line_discount     DECIMAL(10,2) DEFAULT 0,
  line_total        DECIMAL(10,2),
  note              VARCHAR(200)                   -- '不要冰'
);
```

### Discount
```sql
CREATE TABLE discounts (
  name              VARCHAR(80),
  type              VARCHAR(20),                   -- percent / amount / bogo / bundle
  value             DECIMAL(10,2),                 -- 10 = 10% 或 RM10
  code              VARCHAR(40) UNIQUE,            -- 优惠码（null = 手动应用）
  min_subtotal      DECIMAL(10,2),                 -- 满 X 减
  max_discount      DECIMAL(10,2),                 -- 封顶
  applies_to        VARCHAR(20),                   -- all / category / product
  target_ids        JSONB,                         -- 限定 category/product id
  starts_at         TIMESTAMP,
  ends_at           TIMESTAMP,
  usage_limit       INT,
  used_count        INT DEFAULT 0,
  is_stackable      BOOLEAN DEFAULT false,
  is_active         BOOLEAN DEFAULT true
);
```

### Receipt
```sql
CREATE TABLE receipts (
  order_id          UUID UNIQUE REFERENCES orders,
  receipt_no        VARCHAR(30) UNIQUE,            -- 用于 SST 留档
  printed_at        TIMESTAMP,
  reprint_count     INT DEFAULT 0,
  pdf_url           TEXT,                          -- 顾客 App 凭证
  payload           JSONB                          -- 完整快照（防止数据漂移）
);
```

### Employee / Shift / TimeEntry
```sql
CREATE TABLE employees (
  full_name         VARCHAR(80),
  phone             VARCHAR(20),
  email             VARCHAR(120),
  role              VARCHAR(20),                   -- owner/manager/cashier/barista
  pos_pin           CHAR(4),                       -- 4 位 POS 登录码
  hourly_rate       DECIMAL(10,2),
  is_active         BOOLEAN DEFAULT true
);

CREATE TABLE shifts (
  employee_id       UUID,
  store_id          UUID,
  scheduled_start   TIMESTAMP,
  scheduled_end     TIMESTAMP,
  notes             TEXT
);

CREATE TABLE time_entries (
  employee_id       UUID,
  store_id          UUID,
  clock_in_at       TIMESTAMP,
  clock_out_at      TIMESTAMP,
  break_minutes     INT DEFAULT 0,
  total_minutes     INT,
  hourly_rate_snap  DECIMAL(10,2)                  -- 当时时薪快照
);
```

### Inventory / Wastage
```sql
CREATE TABLE inventory_items (
  name              VARCHAR(80),                   -- '鳄梨'
  sku               VARCHAR(50),
  unit              VARCHAR(20),                   -- kg / g / ml / pcs
  cost_per_unit     DECIMAL(10,4),
  current_stock     DECIMAL(12,3),
  reorder_point     DECIMAL(12,3),                 -- 低库存预警
  supplier_id       UUID,
  shelf_life_days   INT                            -- 保质期，触发损耗预警
);

CREATE TABLE recipes (                             -- 产品/Modifier ↔ 原料
  product_id        UUID,
  modifier_id       UUID,
  variant_option_id UUID,
  inventory_item_id UUID,
  quantity_per_unit DECIMAL(12,4)                  -- 1 杯 L 鳄梨汁 = 0.18 kg 鳄梨
);

CREATE TABLE wastage (
  inventory_item_id UUID,
  quantity          DECIMAL(12,3),
  cost_value        DECIMAL(10,2),                 -- 自动算 = qty * cost
  reason            VARCHAR(30),                   -- spoilage/spill/training/complaint
  note              TEXT,
  recorded_by       UUID,
  recorded_at       TIMESTAMP
);
```

### Membership / Loyalty
```sql
CREATE TABLE membership_tiers (
  name              VARCHAR(40),                   -- Silver / Gold / Platinum
  min_spend_year    DECIMAL(10,2),                 -- 升级门槛（年消费）
  point_multiplier  DECIMAL(4,2) DEFAULT 1.0,      -- 积分倍率
  perks             JSONB
);

CREATE TABLE loyalty_rules (
  name              VARCHAR(80),
  earn_rate         DECIMAL(6,4),                  -- 1 RM = 1 分
  redeem_rate       DECIMAL(6,4),                  -- 100 分 = RM 1
  min_redeem_points INT,
  expires_after_days INT,                          -- 一年清零
  is_active         BOOLEAN DEFAULT true
);

CREATE TABLE customer_points (                     -- ledger 形式，可追溯
  customer_id       UUID,
  delta             INT,                           -- +/-
  balance_after     INT,
  source            VARCHAR(20),                   -- earn/redeem/expire/manual
  order_id          UUID,
  expires_at        TIMESTAMP
);
```

### Settings（key-value，灵活扩展）
```sql
CREATE TABLE settings (
  store_id          UUID,
  namespace         VARCHAR(40),                   -- 'tax' / 'receipt' / 'feature'
  key               VARCHAR(60),
  value             JSONB,
  UNIQUE(store_id, namespace, key)
);
```

---

## 3. 销售报表设计（V1 必做 5 张）

| 报表 | 维度 | 核心指标 | 备注 |
|------|------|----------|------|
| **Sales Summary** | 日 / 周 / 月 + 门店 + 渠道 | 总销售、订单数、客单价、毛利率、退款率 | 顶层卡片 + 折线，对比环比 |
| **Sales by Item** | 单品 + 时间 | 销量、销售额、毛利、毛利率、占比 | 排序找畅销/滞销，茶饮店命脉 |
| **Sales by Category** | 品类 | 销售额、占比、同比 | 用来决定下季度菜单调整方向 |
| **Receipts** | 单据 | 收据号、时间、金额、付款方式、员工、状态 | 支持搜索 + 补打，SST 审计也靠它 |
| **Discount Usage** | 折扣名 / Code | 使用次数、让利金额、订单贡献 | 防止员工乱用折扣，老板必看 |

**强烈建议 V1 多加一张「Sales by Hour」** —— 茶饮店的高峰时段直接决定备料和排班，比餐厅敏感得多。Loyverse、Square 都有这一项。

---

## 4. 设置中心子项

```
设置 Settings
├─ 商户资料        — 店名、SSM、地址、Logo
├─ 多门店          — 门店列表（V1 单店，但 schema 多店）
├─ 营业时间        — 周一至六，周日休（按 CLAUDE.md）
├─ 支付方式        — Cash / DuitNow QR / Touch'n Go / GrabPay / Card / 自定义
├─ 税率 / SST      — SST 6%（F&B 现行），可按品类豁免
├─ 服务费          — 占位（学生市场暂不收）
├─ 收据模板        — 顶部 Logo、底部文案、QR 评价
├─ 打印机配置      — 收据打印机 / 制作单打印机（KDS 替代）
├─ 积分 Loyalty    — 1 RM = X 分、过期规则、生日加倍
├─ 会员等级        — Silver / Gold 门槛
├─ 折扣 / 优惠码    — 全局开关、谁能创建
├─ 角色与权限      — 退款、改价、看报表、改菜单
├─ 功能开关        — KDS、自取、外卖、会员、积分（先全建后开关）
├─ 集成            — Grab/Foodpanda（占位）、SQL Accounting（MY 常用，占位）
├─ 通知            — 低库存邮件、日报推送
├─ 账单 & 订阅      — 套餐、付款记录、发票
└─ API & Webhooks  — 给顾客 App 和未来集成用
```

---

## 5. 容易忽略但必须先建好的功能 / 字段

1. **配方表 `recipes`（杯 ↔ 原料）** —— 没有这张表，库存自动扣减和真实毛利率都算不出来。损耗率从 6.8% 降到 1.2% 的关键就在这。**V1 必做**。

2. **损耗 `wastage` + 原因字段** —— 鳄梨、水果是高损耗原料。每天打烊前花 30 秒录入，三个月数据足够支撑采购优化。**V1 必做**。

3. **保质期 `shelf_life_days` + 进货批次** —— 真水果店的命门。先建字段，逻辑可以 V2 再补。

4. **冗余字段 `product_name`、`unit_price` in order_items** —— 历史订单不能因为今天改了菜单就失真。Loyverse 的坑，自研一定要避开。

5. **5sen 凑整 `rounding`** —— 马来西亚 2008 取消 1sen 后，每张单都要凑整到 5sen。Loyverse 海外版偶尔报错，自己存一列最稳。

6. **SST 留档 `receipt_no` 永不重复** —— LHDN/RMCD 查账要求收据号连续、不可改。设计成数据库序列，不允许删除，只能 void。

7. **顾客 `phone` 唯一索引 + 不需要注册即可累积** —— 校园顾客懒得装 App。POS 输入手机号就自动建档积分，是茶饮店复购率提升的最大杠杆。

8. **班次快照 `hourly_rate_snap`** —— 时薪调过之后，老员工的历史工时不能被新时薪覆盖。

9. **`channel` 字段从一开始就有** —— 即使 V1 只有 `pos`，将来接 Grab/Foodpanda 时不用迁移历史数据。报表的渠道维度比想象中重要。

10. **`payload JSONB` in receipts** —— 完整订单快照。出现争议、补打、对账时一秒还原现场。空间换时间，值得。

11. **「试做杯」损耗类型** —— 新员工训练、新品测试都会消耗原料但不产生销售。如果不单列，会被误算成毛利率下降。

12. **Feature Toggles 表** —— 全部模块都先建好但默认 OFF。Andrew 想开就开，不用部署代码。Loyverse、Square 都这么做。

---

## 6. 取舍清单（明确「未来再做」）

| 模块 | V1 | V2 / 未来 |
|------|-----|-----------|
| 多门店 schema | 字段留好 | 第二摊位上线时启用 |
| 外卖平台对接 | channel 字段占位 | 月营业稳定后接 Grab |
| 套餐 / Combo | 不做 | 节庆活动需要时再做 |
| 时段菜单 / Happy Hour | 不做 | 客流数据出来后再设计 |
| KDS 屏幕 | 不做（一个人出品） | 招到第二个 barista 时做 |
| 营销 Campaign 推送 | 不做 | 会员数破 500 再做 |
| 会计软件集成（SQL/Autocount） | 不做 | 月营业额破 RM 30k 再说 |
| 排班自动化 | 手动 Excel 即可 | 员工 ≥ 3 人时再做 |

---

**底层原则**：schema 一次到位，UI 分期实装。Andrew 现在能用上的不超过 30% 的字段，但剩下 70% 的占位让你两年内不用做数据库迁移。这就是「一次到位」最值钱的地方。

---

*调研日期：2026-05-14 | 数据来源：Loyverse / Square / Toast / Eats365 / MenuSifu / Ginger / StoreHub MY / 引瓴数智 等公开资料*
